---
description: Cross-check CI/CD pipeline health (runs, secrets, build-args, branch protection) via the ci-cd-guardian agent
argument-hint: [optional — a specific concern, otherwise runs the full audit]
---

Launch the `ci-cd-guardian` subagent (via the Agent tool, `subagent_type: "ci-cd-guardian"`) to audit pipeline health end-to-end:

$ARGUMENTS
