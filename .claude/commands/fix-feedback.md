---
description: Fix a Page Feedback batch (or a specific item from one) using the ui-feedback-fixer agent
argument-hint: [paste the Page Feedback batch, or describe the specific item to fix]
---

Launch the `ui-feedback-fixer` subagent (via the Agent tool, `subagent_type: "ui-feedback-fixer"`) to handle the following, end-to-end (diagnose to exact source line, fix, verify via tsc/eslint, report back):

$ARGUMENTS
