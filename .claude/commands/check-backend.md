---
description: Answer a question about real backend behavior by reading BackendEasy's source, via the backend-liaison agent
argument-hint: [the endpoint/contract/behavior question]
---

Launch the `backend-liaison` subagent (via the Agent tool, `subagent_type: "backend-liaison"`) to answer this by reading `../BackendEasy`'s actual source (controllers, DTOs, migrations, `SESSION-HANDOFF.md`) rather than guessing from frontend-side symptoms:

$ARGUMENTS
