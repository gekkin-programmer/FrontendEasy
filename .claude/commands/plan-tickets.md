---
description: MVP-oriented planning and JIRA ticket creation for colleagues, plus feedback triage — via the product-planner agent (replies in French)
argument-hint: [optional — a feedback batch to triage, or a focus area, otherwise does a general sweep]
---

Launch the `product-planner` subagent (via the Agent tool, `subagent_type: "product-planner"`) to figure out what should be done next (MVP-first, asking before assuming scope), triage feedback, and create JIRA tickets for colleagues:

$ARGUMENTS
